{
  "name": "claude-test",
  "version": "3.0.0",
  "description": "紹介制マッチングアプリ（紹介文必須・本人確認必須・30歳以上）",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "seed": "node scripts/seed.js",
    "test": "jest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
